{%- comment -%}
  Maison Cavallé — shop policies

  Shopify only accepts a Liquid template here; `policy` is not one of the
  template types that can be JSON. The section is still static, so it stays
  editable in the Theme Editor.

  One template covers every policy in Settings → Policies: shipping, refund,
  privacy, terms and contact information.
{%- endcomment -%}
{% section 'jci-policy' %}
